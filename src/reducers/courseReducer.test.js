import courseReducer from './courseReducer';
import { FETCH_COURSES, LOAD_STUDENTS_FOR_COURSE } from '../actions/types';
import { courses, students } from '../tools/mockData';

describe('Course Reducer', () => {
  it('should take courses when passed FETCH_COURSE', () => {
    // Self-paced
  });

  it('should take course students when passed LOAD_STUDENTS_FOR_COURSE', () => {
    // Self-paced
  });
});
