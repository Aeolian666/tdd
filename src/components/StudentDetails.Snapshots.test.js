import React from 'react';
import renderer from 'react-test-renderer';
import StudentDetails from './StudentDetails';
import { address } from '../tools/mockData';

it('display address info when there is one', () => {
  // ** Code along
});
